<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>eBook Library Service</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        body {
            background: linear-gradient(to bottom right, #E6F3FF, #B0E0E6);
            min-height: 100vh;
            font-family: 'Poppins', sans-serif;
            color: #333;
            margin: 0;
        }

        .custom-container {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            background: rgba(255, 255, 255, 0.9);
            padding: 40px 60px;
            border-radius: 20px;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
            animation: fadeIn 1s ease-in-out;
            height: 100vh;
        }


        .custom-title {
            color: #4DA8DA;
            font-weight: bold;
            font-size: 2.5rem;
        }

        .custom-subtitle {
            color: #1E90FF;
            font-size: 1.2rem;
            margin-bottom: 2rem;
        }

        .btn-custom {
            background: linear-gradient(135deg, #4DA8DA, #1E90FF);
            border: none;
            color: white;
            font-size: 1.25rem;
            padding: 0.75rem 1.5rem;
            margin: 0.5rem;
            border-radius: 30px;
            transition: transform 0.3s ease-in-out, box-shadow 0.3s ease-in-out;
            display: inline-flex;
            align-items: center;
            gap: 10px;
        }

            .btn-custom:hover {
                transform: translateY(-5px);
                box-shadow: 0 8px 20px rgba(0, 0, 0, 0.2);
            }

       

     
    </style>
</head>
<body>
    <div class="custom-container">
        <h1 class="custom-title">Welcome to the eBook Library Service</h1>
        <p class="custom-subtitle">Your gateway to endless reading possibilities</p>
        <div class="buttons">
            <a href="/HomePage/SignUp" class="btn btn-custom">
                <i class="fas fa-user-plus"></i> Sign up
            </a>
            <a href="/HomePage/SignIn" class="btn btn-custom">
                <i class="fas fa-sign-in-alt"></i> Sign in
            </a>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>
