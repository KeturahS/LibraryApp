<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Book Waiting List</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #E6F3FF;
        }
        .navbar {
            background-color: #4DA8DA !important;
        }
        .card {
            background-color: #F0F8FF;
            border: 1px solid #87CEFA;
        }
        .card-header {
            background-color: #B0E0E6;
            color: #00008B;
        }
        .btn-primary {
            background-color: #4DA8DA;
            border-color: #4DA8DA;
        }
        .btn-primary:hover {
            background-color: #1E90FF;
            border-color: #1E90FF;
        }
        .btn-secondary {
            background-color: #87CEFA;
            border-color: #87CEFA;
            color: #00008B;
        }
        .btn-secondary:hover {
            background-color: #4DA8DA;
            border-color: #4DA8DA;
            color: white;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-dark">
        <div class="container">
            <span class="navbar-brand mb-0 h1">Library Management System</span>
        </div>
    </nav>

    <div class="container mt-4">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card">
                    <div class="card-header">
                        <h2 class="card-title mb-0">Book Waiting List</h2>
                    </div>
                    <div class="card-body">
                        <h3 class="mb-3">Book Details</h3>
                        <p><strong>Title:</strong> The Great Gatsby</p>
                        <p><strong>Author:</strong> F. Scott Fitzgerald</p>
                        <p><strong>ISBN:</strong> 978-0743273565</p>
                        <p><strong>Publication Year:</strong> 1925</p>

                        <hr>

                        <h3 class="mb-3">Waiting List Information</h3>
                        <p><strong>People on Waiting List:</strong> 5</p>
                        <p><strong>Estimated Availability:</strong> 15 days</p>

                        <hr>

                        <h3 class="mb-3">Join Waiting List</h3>
                        <form id="waitingListForm">
                            <div class="mb-3">
                                <label for="name" class="form-label">Name</label>
                                <input type="text" class="form-control" id="name" required>
                            </div>
                            <div class="mb-3">
                                <label for="email" class="form-label">Email</label>
                                <input type="email" class="form-control" id="email" required>
                            </div>
                            <button type="submit" class="btn btn-primary">Join Waiting List</button>
                        </form>

                        <div class="alert alert-info mt-3" role="alert">
                            <strong>Note:</strong> Once you join the waiting list, you will be notified by email when the book becomes available for you to borrow.
                        </div>

                        <a href="#" class="btn btn-secondary mt-3">Return to Home Page</a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        document.getElementById('waitingListForm').addEventListener('submit', function(e) {
            e.preventDefault();
            alert('You have been added to the waiting list. You will be notified by email when the book is available.');
            this.reset();
        });
    </script>
</body>
</html>